import React from 'react';

export const id = 'if-else',
    name = 'Логические операторы (if/else)';

export const Definition = () => (
    <>
        <em>{name}</em> - слова стоящие перед абзацем, которые указывают, что часть действий,
        описанных в функции, должна выполняться не всегда, а только при каких-то условиях.
    </>
);
