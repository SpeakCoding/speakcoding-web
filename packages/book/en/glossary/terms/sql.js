import React from 'react';

export const id = 'sql',
    name = 'SQL (Structured Query Language)';

export const Definition = () => (
    <p>
        <em>{name}</em> - used when programs interact with databases. Used when programs interact
        with databases. Any such interaction happens when a database is sent a request to perform a
        particular action (create, read, change, or delete data).
    </p>
);
