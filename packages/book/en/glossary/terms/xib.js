import React from 'react';

export const id = 'xib',
    name = '.xml/.html/.xib';

export const Definition = () => (
    <p>
        <em>{name} files</em> - files with visual editors for use with interface elements.
    </p>
);
