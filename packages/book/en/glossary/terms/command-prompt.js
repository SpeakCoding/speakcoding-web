import React from 'react';

export const id = 'command-prompt',
    name = 'Command prompt (Terminal)';

export const Definition = () => (
    <>
        a standalone piece of software that is part of the Operating System and facilitates
        interaction between the user and the Operating System.
    </>
);
