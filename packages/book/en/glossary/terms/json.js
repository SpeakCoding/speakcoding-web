import React from 'react';

export const id = 'json',
    name = 'JSON';

export const Definition = () => (
    <>
        a convenient format for recording information about objects in textual form. Can be
        presented as a named set for which each name corresponds to the name of a component.
    </>
);
