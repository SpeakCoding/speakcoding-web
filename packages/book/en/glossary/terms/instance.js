import React from 'react';

export const id = 'instance',
    name = 'Instance of a class (object)';

export const Definition = () => (
    <p>
        <em>{name}</em> - an instance of one of the elements that we create when writing the
        program. Any specific post can serve as an example of the Post class.
    </p>
);
