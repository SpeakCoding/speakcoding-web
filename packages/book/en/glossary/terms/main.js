import React from 'react';

export const id = 'main',
    name = 'Main';

export const Definition = () => (
    <>
        the initial function that launches the entire program, and the point where all operations
        begin. This is a block inside which all procedures are described that are needed to launch
        programs, as are the main views that the user should see when first interacting with the
        program (such as displaying LoginView).
    </>
);
