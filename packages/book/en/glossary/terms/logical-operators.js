import React from 'react';

export const id = 'logical-operators',
    name = 'Logical operators (if/else)';

export const Definition = () => (
    <p>
        <em>{name}</em> - words appearing before a paragraph that indicate that some of the actions
        defined in the function should only be performed under certain conditions.
    </p>
);
