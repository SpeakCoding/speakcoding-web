import React from 'react';

export const id = 'closure',
    name = 'Closure';

export const Definition = () => (
    <>
        <em>{name}</em> - a function of "completion" или "closure" of some action/event. In code it
        is usually located inside another function, and must be executed before the compiler
        continues to read the code further.
    </>
);
