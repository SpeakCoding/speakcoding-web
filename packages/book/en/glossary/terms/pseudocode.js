import React from 'react';

export const id = 'pseudocode',
    name = 'Pseudocode';

export const Definition = () => (
    <p>
        <em>{name}</em> - a plain language description of the steps in an algorithm, intended for
        human reading rather than machine reading.
    </p>
);
