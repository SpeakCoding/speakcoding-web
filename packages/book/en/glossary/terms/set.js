import React from 'react';

export const id = 'set',
    name = 'Set';

export const Definition = () => (
    <p>
        <em>{name}</em> - a standard class that combines an indefinite number of a particular
        class’s elements.
    </p>
);
