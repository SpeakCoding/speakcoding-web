import React from 'react';

export const id = 'parent-class',
    name = 'Parent class';

export const Definition = () => (
    <p>
        <em>{name}</em> - a class from which other classes inherit their components, properties, and
        functions.
    </p>
);
