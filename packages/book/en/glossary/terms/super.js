import React from 'react';

export const id = 'super',
    name = 'Super';

export const Definition = () => (
    <>
        keyword, means ‘include everything that is described in the parent class’. Add this word if:
        1) you do not know the exact definition of a class; or 2) you wish to override a particular
        function using existing functionality. You do not need to add the word super if you re-write
        the function.
    </>
);
