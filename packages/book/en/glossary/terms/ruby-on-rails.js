import React from 'react';

export const id = 'ruby-on-rails',
    name = 'Ruby on Rails';

export const Definition = () => (
    <p>
        <em>{name}</em> - a framework (program platform) that has standardized the principles of
        storing data as well as naming classes and functions and, accordingly, the files and folders
        in which they are stored on the server.
    </p>
);
