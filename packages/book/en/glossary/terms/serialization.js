import React from 'react';

export const id = 'serialization',
    name = 'Serialization';

export const Definition = () => (
    <p>
        <em>{name}</em> - a procedure for recording information in text form about instances of a
        particular class.
    </p>
);
