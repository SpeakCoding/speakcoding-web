import React from 'react';

export const id = 'ui-kit',
    name = 'UI Kit (User Interface Kit)';

export const Definition = () => (
    <>
        a standard library of interface elements; used particularly when developing apps in the iOS
        operating system.
    </>
);
