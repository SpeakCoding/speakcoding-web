import React from 'react';

export const id = 'deserialization',
    name = 'Deserialization';

export const Definition = () => (
    <>
        the reverse operation to serialization; this entails reading a file containing data about an
        instance of a particular class, then ‘populating’ this saved data into a template for the
        reconstructed instance and its components.
    </>
);
