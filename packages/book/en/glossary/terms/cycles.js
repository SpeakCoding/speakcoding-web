import React from 'react';

export const id = 'cycles',
    name = 'Cycles';

export const Definition = () => (
    <>
        used when an action defined in a paragraph needs to be repeated multiple times; the number
        of times is typically specified by a value, enclosed in round brackets ( ), that appears
        after the pointer words ‘for’ or ‘while’ and before the start of the paragraph.
    </>
);
